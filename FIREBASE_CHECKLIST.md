# Firebase Setup Checklist

Use this checklist to verify your Firebase setup is complete.

## ✅ Configuration Files

- [x] `android/app/google-services.json` - Present and valid
- [ ] `ios/Runner/GoogleService-Info.plist` - Only needed if building for iOS
- [ ] `lib/firebase_options.dart` - Optional (generated by `flutterfire configure`)

## ✅ Firebase Console Setup

- [ ] Firebase project created
- [ ] Email/Password authentication enabled
  - Go to: Authentication → Sign-in method → Email/Password → Enable
- [ ] Cloud Firestore database created
  - Go to: Firestore Database → Create database → Start in test mode

## ✅ Android Configuration

- [x] `google-services.json` in `android/app/`
- [x] Google Services plugin in `android/app/build.gradle.kts`
- [x] Plugin version in `android/settings.gradle.kts`
- [x] Package name matches: `com.ProgramEz.Quizzit`

## ✅ Dependencies

- [x] `firebase_core: ^3.6.0` - Installed
- [x] `firebase_auth: ^5.3.1` - Installed
- [x] `cloud_firestore: ^5.4.4` - Installed

## ✅ Code Configuration

- [x] `lib/main.dart` - Firebase initialized
- [x] `lib/screens/login_screen.dart` - Created
- [x] `lib/screens/signup_screen.dart` - Created
- [x] Authentication flow implemented

## 🧪 Testing

After completing the Firebase Console setup:

1. Run the app: `flutter run`
2. Try signing up with a new email
3. Check Firebase Console → Authentication → Users
4. Try logging out and logging back in

## 📝 Next Steps

1. **Complete Firebase Console Setup** (if not done):
   - Create/select Firebase project
   - Enable Email/Password authentication
   - Create Firestore database

2. **Verify google-services.json**:
   - Ensure it matches your Firebase project
   - If you created a new project, download a new `google-services.json`

3. **Test Authentication**:
   - Run the app and test signup/login flow

4. **Optional - Use FlutterFire CLI**:
   ```bash
   dart pub global activate flutterfire_cli
   flutterfire configure
   ```
   Then update `lib/main.dart` to use `DefaultFirebaseOptions.currentPlatform`

## 🆘 Common Issues

### "FirebaseApp not initialized"
- Ensure `google-services.json` is in `android/app/`
- Verify package name matches Firebase Console
- Run `flutter clean` and rebuild

### "Email/Password not enabled"
- Go to Firebase Console → Authentication → Sign-in method
- Enable Email/Password provider

### Build errors
- Run `flutter clean`
- Run `flutter pub get`
- For Android: `cd android && ./gradlew clean && cd ..`
- For iOS: `cd ios && pod install && cd ..`

## 📚 Documentation

See `FIREBASE_SETUP.md` for detailed setup instructions.

